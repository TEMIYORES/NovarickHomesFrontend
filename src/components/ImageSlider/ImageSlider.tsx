import "./imageslider.scss";
// import { Img50, Img51, Img52, Img53, Img54, Img55 } from "../../assets/images";

const ImageSlider = () => {
  return (
    <>
      <div>Carousel</div>
      {/* <Carousel className="slider" breakPoints={breakpoints} pagination={false}>
        {images.map((img, idx) => (
          <div className={idx === imageIndex ? "slide activeSlide" : "slide"}>
            <img src={img} alt={img} />
          </div>
        ))}
      </Carousel> */}
    </>
  );
};

export default ImageSlider;
